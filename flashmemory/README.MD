FIRST upload main.cpp 
then cd into scripts and run python_audio.py in the terminal...press the boot button on esp32 as the script is searchng for esp32
it will upload all te data into the flash memory